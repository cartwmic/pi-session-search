## 0. API verification (must complete before any implementation)

- [x] 0.1 **⚠️ Wave-1 deviation note**: `ctx.modelRegistry.getApiKeyAndHeaders(model)` does NOT exist on installed pi-coding-agent. Actual API: `ctx.modelRegistry.getApiKey(model): Promise<string | undefined>`; headers come from `model.headers?: Record<string, string>` directly. Better still: pi-ai's `complete(model, ctx, opts)` resolves auth internally from the Model object (which carries provider+baseUrl+headers) — the digest builder does NOT need to manually inject auth. All other surfaces verified present (events, ui.custom<T>(factory, opts), sessionManager.getBranch/getSessionId, modelRegistry.getAvailable, switchSession, setSessionName/getSessionName).
- [x] 0.1-orig Verify the following ExtensionAPI surface against installed `@mariozechner/pi-coding-agent` (target version range: `>=0.65.2 <0.67.0`; 0.66.1 was used to draft these specs and 0.65.2 was empirically resolvable on the dev host): `pi.on("agent_end", ...)`, `pi.on("session_compact", ...)`, `pi.on("session_shutdown", ...)`, `pi.on("session_start", ...)`, `pi.setSessionName(name)`, `pi.getSessionName()`, `ctx.sessionManager.getBranch()`, `ctx.sessionManager.getSessionId()`, `ctx.modelRegistry.getAvailable()`, `ctx.modelRegistry.getApiKeyAndHeaders(model)`, `ctx.switchSession(path)` (on `ExtensionCommandContext` only), **and `ctx.ui.custom(factory, opts?)` with the `done(value)` callback contract** (used by `/find-session` overlay). Cmd: `grep -nE 'on\(event:\s*"(agent_end|session_compact|session_shutdown)"|custom<' node_modules/@mariozechner/pi-coding-agent/dist/core/extensions/types.d.ts`. **Stop-gate**: if `ctx.ui.custom` is missing or has a different signature, halt the overlay implementation (tasks 9.x) and fall back to a `ctx.ui.select`-based picker; document the fallback in design.md before proceeding.
- [x] 0.2 Verify pi-ai's tool-call surface for the digest builder. Cmd: `grep -nE 'export interface Tool|interface Context|interface ToolCall' node_modules/@mariozechner/pi-ai/dist/types.d.ts`. Required: `Tool<TParameters extends TSchema>` exists with `{name, description, parameters}` shape; `Context.tools?: Tool[]` accepts the tool list; `AssistantMessage.content` includes `{type: "toolCall", name, arguments: Record<string, any>}` entries. **Empirically verified on dev host**: all three exist in pi-ai 0.5.x. The digest builder uses `complete(model, {systemPrompt, messages, tools: [submitDigestTool]}, opts)` and reads tool calls from `response.content`.
- [x] 0.3 Verify `complete` is exported from `@mariozechner/pi-ai` AND that it accepts `signal: AbortSignal` (for the 60s hard-timeout in task 4.9). Cmd: `node -e "const m=require('@mariozechner/pi-ai'); console.log(typeof m.complete)"`; then check `complete()`'s `options` parameter accepts `signal` against `node_modules/@mariozechner/pi-ai/dist/stream.d.ts` (`StreamOptions.signal?: AbortSignal` is documented). If absent or differently named, redesign 4.9 to use `Promise.race(promise, new Promise((_, rej) => setTimeout(rej, 60000)))` with best-effort cancellation.
- [x] 0.4 Verify `Model<Api>` field shape against installed pi-ai. Cmd: `grep -A12 '^export interface Model' node_modules/@mariozechner/pi-ai/dist/types.d.ts`. Required fields: `cost.input`, `cost.output` (per-token unit prices, used by `/digest:backfill --dry-run`); `contextWindow` (used by `capInputForFullMode`); `provider`, `id`. If `contextWindow` is absent, fall back to a flat 100000-char cap and document; if `cost` shape differs, update task 8.6 formula before implementing.
- [x] 0.5 Dump the live model registry on the dev host to confirm priority-list IDs actually resolve. Cmd: `pi --list-models | head -50` OR a node one-liner against the model registry. Pin the priority list in `digest/model-resolver.ts` to IDs that empirically resolve. If none of `[gpt-5.4-nano, gpt-5.4-mini, claude-4-5-haiku, gemini-3-flash]` are present, replace with the closest available cheap-class IDs and update spec accordingly. The fallback notification ("none of [<priority>] are configured") SHALL include a comma-separated list of the actually-available cheap-class models from `getAvailable().filter(m => m.cost.input < 0.5)` (or similar threshold) so the user knows what to put in `digest.json`.
- [x] 0.6 Verify `@mariozechner/pi-tui` exports `Component`, `Container`, `Focusable`, and `OverlayHandle` (used by `/find-session` overlay in tasks 9.x). Cmd: `grep -nE 'export (class|interface|type) (Component|Container|Focusable|OverlayHandle)' node_modules/@mariozechner/pi-tui/dist/*.d.ts`. Stop-gate on absence — if any are missing, the overlay needs a different rendering approach.

## 1. Embedder simplification (no dependencies)

- [x] 1.1 Remove `BedrockEmbedder` class from `src/embedder.ts`
- [x] 1.2 Remove `OllamaEmbedder` class from `src/embedder.ts`
- [x] 1.3 Remove `MistralEmbedder` from `src/embedder.ts` (if separate; otherwise drop the mistral branch in `createEmbedder`)
- [x] 1.4 Collapse `EmbedderConfig` to the new shape: `{baseUrl, model, apiKey?, apiKeyEnv?, dimensions?, headers?}` — drop the `type` discriminator and provider-specific fields (`profile`, `region`, `url`)
- [x] 1.5 Rename `createEmbedder()` to `createEmbedder(config: EmbedderConfig): Embedder` returning the single `OpenAICompatibleEmbedder`
- [x] 1.6 Add `apiKeyEnv` resolution logic: if `apiKey` is unset and `apiKeyEnv` is set, read `process.env[apiKeyEnv]`; if both are unset, fall back to `process.env.OPENAI_API_KEY`. **If after this resolution the effective key is empty/undefined**, refuse to construct the embedder and emit `ctx.ui.notify("session-search: embedder configured but no API key resolvable. Set apiKey, apiKeyEnv, or OPENAI_API_KEY in env. Falling back to fts-raw mode.", "warning")`; the extension proceeds in `fts-raw` mode for this session.
- [x] 1.7 Add `headers?: Record<string, string>` passthrough to fetch calls (merged with `Authorization` and `Content-Type`)
- [x] 1.8 Remove `@aws-sdk/client-bedrock-runtime` and `@aws-sdk/credential-providers` from `package.json` `optionalDependencies`
- [x] 1.9 Run `npm install` and confirm AWS SDK packages are no longer in the resolved tree
- [x] 1.10 Update `src/__tests__/index.test.ts`: extend embedder tests for dimensions passthrough, apiKeyEnv resolution, and custom-headers merging
- [x] 1.11 Add legacy-config migration: on load, if `embedder.type !== undefined && embedder.type !== "openai-compatible"`, log warning and refuse to construct embedder until `/session-embeddings-setup` reruns; if `type === "openai-compatible"`, silently strip the field
- [x] 1.12 Verify `node --test --import tsx src/**/*.test.ts` passes

## 2. Digest module — pure / no-event-bus

- [x] 2.1 Create `src/digest/schema.ts` exporting the `SessionDigest` type and a TypeBox-based `validateDigest(obj: unknown): SessionDigest | null` validator (consistent with the project's existing `@sinclair/typebox` dependency for tool schemas)
- [x] 2.2 Create `src/digest/storage.ts`:
  - `digestPath(sessionId: string): string` → `~/.pi/session-search/digests/<id>.json`
  - `loadDigest(sessionId): SessionDigest | null` (returns null if missing or invalid)
  - `saveDigest(sessionId, digest): void` (atomic: write to `<path>.tmp`, then `rename`)
  - `listDigestedSessionIds(): string[]`
  - **Builder state persistence** (sibling file): `statePath(sessionId): string` → `~/.pi/session-search/digests/<id>.state.json`. `loadBuilderState(sessionId): {convTokensAtLastWrite, lastWrittenMessageIndex, lastWrittenSummaryIndex} | null`. `saveBuilderState(sessionId, state): void` (atomic). State is reloaded on `session_start` so incremental-prompt anchors survive process restarts — without this, after every reload the next `agent_end` would either trigger a wasteful full re-summarize (if convTokens=0 < threshold) or send the entire conversation as "delta" (if anchor=0).
- [x] 2.3 Create `src/digest/cost-tracker.ts`:
  - `CostRollup` type with `{calls, tokensIn, tokensOut, cost: {input, output, cacheRead, cacheWrite, total}}`
  - `record(rollup, response): CostRollup` (pure)
  - `format(rollup, modelName): string` → the line shown by `/digest:cost`
- [x] 2.4 Create `src/digest/model-resolver.ts`:
  - `AUTO_DETECT_MODELS: string[] = ["gpt-5.4-nano", "gpt-5.4-mini", "claude-4-5-haiku", "gemini-3-flash"]`
  - `resolveModel(config, registry): {provider, model} | undefined` honoring explicit override; falling back to priority-list scan
  - **⚠️ Wave-1 deviation note (task 0.5)**: of the four priority-list IDs, only `gpt-5.4-mini` resolves exactly on the dev host (via `openai-codex` provider). The other three do NOT match any `Model.id` exactly. Empirically-resolvable replacements from `pi --list-models` (2026-04-29): `"openai/gpt-5.4-nano"` (openrouter), `"gpt-5.4-mini"` (openai-codex ✅ already resolves), `"claude-haiku-4-5"` (claude-bridge), `"google/gemini-3-flash-preview"` (openrouter). Update `AUTO_DETECT_MODELS` to `["openai/gpt-5.4-nano", "gpt-5.4-mini", "claude-haiku-4-5", "google/gemini-3-flash-preview"]` when implementing this task. For the "none configured" notification text, cheap-class models available on this host (cost.input < 0.5/MTok class): `openai/gpt-5.4-nano`, `gpt-5.4-mini`, `openai/gpt-5.4-mini`, `claude-haiku-4-5`, `anthropic/claude-haiku-4.5`, `google/gemini-2.5-flash-lite`, `google/gemini-2.5-flash`, `google/gemini-3-flash-preview`, `~google/gemini-flash-latest`, `~openai/gpt-mini-latest`.
- [x] 2.5 Create `src/digest/conversation-view.ts`: `interface ConversationView { messages: Array<{role: 'user'|'assistant', text: string}>, compactionSummaries: string[] }`. Two adapters: `liveConversationView(ctx)` (uses `ctx.sessionManager.getBranch()` — returns the **active branch** by definition) and `parsedConversationView(parsed: ParsedSession)` (uses the parser's flat `userMessages` + `assistantText` + `compactionSummaries` output — the existing parser does NOT preserve branch graph topology, so for sessions with multiple branches the backfill view contains messages from all branches in JSONL order). **Known limitation**: live and backfill views diverge for branched sessions. The first live `agent_end` after backfill rewrites the digest using the active-branch view, self-correcting. Document the limitation in a code comment + reference design.md "Backfill data path" decision. (Alternative considered + rejected this round: extending `ParsedSession` with branch lineage — costs parser changes + storage growth for an uncommon case.)
- [x] 2.6 Create `src/digest/builder.ts`:
  - `interface BuilderState { lastDigest, convTokensAtLastWrite, lastWrittenMessageIndex, lastWrittenSummaryIndex, lastWriteTime, pendingCall, dirty }`. **`lastWrittenMessageIndex`** is the index into the `ConversationView.messages` array at the time of the last successful digest write — the **delta anchor** used by incremental prompts. **`lastWrittenSummaryIndex`** is the corresponding anchor for `compactionSummaries[]` so new summaries between digest writes are surfaced in incremental mode.
  - `estimateTokens(text: string): number` — `Math.ceil(text.length / 4)`, matching pasky
  - `extractDelta(view: ConversationView, anchor: number): ConversationView` — returns a new view containing only `messages[anchor..]`. Anchor `0` returns the full view (used for full re-summarize).
  - `capInput(view, model, includesPrevDigest: boolean): ConversationView` — if `serializedLength > min(100000, model.contextWindow * 4 - maxTokens * 4 - (includesPrevDigest ? 4000 : 2000))`, truncate by KEEP-ing all `compactionSummaries` + first user message + most-recent-N messages until under cap. Drop middle. **Applied to both full and incremental modes** (incremental's combined input = prev-digest + delta; budget reserves 4000 chars envelope when prev-digest is included). If `model.contextWindow` is absent on this pi-ai version, use flat 100000 cap regardless.
  - **Tool definition** (in `digest/schema.ts`): `submitDigestTool: Tool<typeof DigestArgs>` where `DigestArgs = Type.Object({body: Type.String({minLength: 50}), headline: Type.String({minLength: 1, maxLength: 80}), topics: Type.Array(Type.String({maxLength: 32}), {maxItems: 5}), outcome: Type.Optional(Type.String({maxLength: 200}))})`.
  - `buildPrompt(state, view: ConversationView, threshold, model): {mode: "incremental"|"full", systemPrompt: string, userMessage: string}` (pure). System prompt instructs the LLM to call `submit_digest` exactly once with the digest content. User message contains conversation. In `incremental` mode, user message includes `state.lastDigest.body` AND `capInput(extractDelta(view, state.lastWrittenMessageIndex), model, true)`. In `full` mode, includes `capInput(view, model, false)` and ignores `lastDigest`.
  - `async generateDigest(model, view, prevDigest, opts): Promise<{digest: SessionDigest, anchor: number} | null>` calling `complete(model, {systemPrompt, messages: [{role:'user', ..., content: [{type:'text', text: userMessage}]}], tools: [submitDigestTool]}, {signal, ...})` from `@mariozechner/pi-ai`. Reads `response.content` for the first `{type: "toolCall", name: "submit_digest"}` entry; takes its `arguments` as the digest payload; decorates with metadata fields (`schemaVersion`, `generatedAt`, `modelId`, `inputTokenCount`, `cost: response.usage.cost.total`) to produce the final `SessionDigest`. Returns `{digest, anchor: view.messages.length}`. On failure modes (no tool call, wrong tool name, args fail validation): retry once with a stricter prompt; on second failure return null.
  - Builder is event-system-agnostic and source-agnostic — takes a `ConversationView` not a pi `ctx`; lifecycle wiring composes them
- [x] 2.7 Add unit tests under `src/__tests__/digest/`:
  - `schema.test.ts` — validate/reject malformed; near-cap output
  - `storage.test.ts` — atomic-write roundtrip; load returns null on missing/corrupt files
  - `cost-tracker.test.ts` — accumulation arithmetic; `format` output (includes "this process" wording)
  - `model-resolver.test.ts` — explicit-override path; priority-scan path; tie-break by lowest-cost-provider on ambiguous match; no-match returns undefined
  - `builder.test.ts` — `buildPrompt` mode selection at threshold boundary; first-digest-is-full; `extractDelta` correctness across anchor boundaries; `capInput` keeps compaction summaries + head + tail when over cap (both full and incremental modes); `generateDigest` with mocked `complete()` happy path (single `submit_digest` tool call); LLM-emits-text-instead-of-tool-call retry path; multiple-tool-calls-take-first path; double-failure drop path; `estimateTokens` arithmetic
  - `conversation-view.test.ts` — live adapter and parsed adapter both produce equivalent shapes for the same non-branched session. On a branched session, the live adapter (`getBranch()`) returns only the active branch while `parsedConversationView` returns the parser's flat output across all branches; the test asserts this expected divergence and documents that the next live `agent_end` rewrites the digest to the active-branch view (per `D13` / branches-not-special-cased decision in design.md).
- [x] 2.8 Verify all digest tests pass

## 3. Digest config

- [x] 3.1 Create `~/.pi/session-search/digest.json` schema and loader: `loadDigestConfig(cwd: string): DigestConfig` reading global file, then merging `<cwd>/.pi/session-search/digest.json` on top
- [x] 3.2 Default values: `debounceSeconds: 60, resummarizeTokenThreshold: 10000, maxTokens: 1500, showWidget: false, verbose: false`
- [x] 3.3 `saveDigestConfig(config)` writes the global file (used by `/digest:settings`)
- [x] 3.4 Unit tests: defaults applied when file absent; project override merges on top; malformed file falls back to defaults with a warning

## 4. Digest lifecycle wiring

- [x] 4.1 Create `src/digest/lifecycle.ts` exporting `installDigestLifecycle(pi: ExtensionAPI, deps: {storage, builder, costTracker, configLoader, modelResolver, indexAddDigested}): {dispose: () => void}`
- [x] 4.2 Wire `pi.on("session_start", ...)`: reload config, restore in-memory state from `lastDigest = loadDigest(sessionId)` AND `loadBuilderState(sessionId)` (so anchors survive restart), kick off no LLM calls. **Notification scope**: maintain an in-memory `notifiedThisProcess: boolean` (module-scoped, NOT session-scoped). When `digestRequested === true` AND `resolveModel(...)` returns undefined AND `notifiedThisProcess === false`, emit `ctx.ui.notify("session-search: digest mode unavailable — none of [<priority>] are configured (available cheap models: <list>). Running in hybrid-raw mode.", "warning")` and set the flag. The flag resets on extension reload (a new module load gives a new flag value).
- [x] 4.3 Wire `pi.on("agent_end", ...)`: increment turn counter; if `Date.now() - lastWriteTime >= debounceSeconds * 1000` AND `pendingCall === false`, kick off `generateDigest()` via the live `ConversationView` (fire-and-forget). If `pendingCall === true`, set `dirty = true` and return.
- [x] 4.4 Wire `pi.on("session_compact", ...)`: clear any pending debounce timer (so the trailing debounce fire doesn't add a redundant call after the compact-triggered one); bypass debounce; if `pendingCall === false`, kick off `generateDigest()`. If `pendingCall === true`, set `dirty = true`.
- [x] 4.5 Wire `pi.on("session_shutdown", ...)`: abort any in-flight LLM call via AbortController; clear timers; clear `dirty` flag.
- [x] 4.6 On successful digest: `saveDigest(id, digest)`; `pi.setSessionName(digest.headline)`; update in-memory `lastDigest` + `lastWriteTime` + `convTokensAtLastWrite` + `lastWrittenMessageIndex` + `lastWrittenSummaryIndex`; **call `saveBuilderState(sessionId, {convTokensAtLastWrite, lastWrittenMessageIndex, lastWrittenSummaryIndex})`** (atomic sibling-file write, so anchors survive process restart — without this call, every restart wastes tokens via spurious full re-summarize or whole-conversation incremental delta); record cost; call `indexAddDigested(...)` (with `batched: false` for live updates) so search index updates immediately.
- [x] 4.7 On failed digest (no `submit_digest` tool call after retry, tool args fail TypeBox validation after retry, or API error): set `lastError`, leave prior digest untouched, do NOT call `setSessionName`.
- [x] 4.8 **Coalescing rule** (single source of truth): if a trigger fires while `pendingCall === true`, drop the trigger AND set `dirty = true`. When the in-flight call completes (success or failure), if `dirty === true`, schedule ONE follow-up `generateDigest()` after a 250ms tail delay and clear `dirty`. Never queue more than one follow-up.
- [x] 4.9 Hard timeout on every LLM call: 60-second AbortController timeout; on timeout, treat as failure (per 4.7).
- [x] 4.10 Unit-test the lifecycle wiring with a fake `pi` event emitter and mocked `generateDigest` — verify debounce, coalescing-with-deferred-flush, deferred-flush-cleared-on-shutdown, abort-on-shutdown, hard-timeout, **state-persists-across-simulated-restart** (write a digest → re-instantiate the lifecycle with a fresh in-memory state → confirm `loadBuilderState` repopulates `convTokensAtLastWrite`/anchors so the next `agent_end` triggers incremental, not full).

## 4.5. Mode detection re-evaluation

- [x] 4.5.1 `detectMode(config, digestModelResolved): Mode` is called from `session_start`. Because `ctx.modelRegistry.getAvailable()` may not have fully populated yet during early extension load, the lifecycle SHALL re-evaluate ONCE: if the initial detection yields `hybrid-raw`/`fts-raw` AND `digestRequested === true`, schedule a single retry via `setTimeout(reEvaluate, 1000)`. If the second detection yields `digest-mode`: (a) if the active index has zero entries (fresh install), simply switch the active index instance to `SessionIndex` in digest-mode and install the digest lifecycle; (b) if the active index already has hybrid-raw entries (raw-content embeddings), mark all entries dirty for re-embed (same mechanism as `vectorDim` mismatch in task 5.9) AND **clear each entry's `embedding` field to empty/null so the search-filter invariant from task 6.11 holds during the transitional window** (entries with empty embedding == un-digested in digest-mode); do not retain the old raw-content vector. Install the digest lifecycle, and let the next `sync()` re-embed against `digest.body` after digests exist (which only happens after `/digest:backfill` runs or new sessions complete). **Never** silently mix raw-content and digest-content embeddings in the same vector space — cosine scores would be incomparable. If still not `digest-mode` after retry, emit the fallback notification (per task 4.2) and stay in the current mode for the remainder of this process; do not re-evaluate again.
- [x] 4.5.2 Unit-test the re-evaluation: with a mock registry that returns empty on first call and the priority model on second call, verify the lifecycle upgrades to `digest-mode` after the 1s retry.

## 5. Indexing layer rewrite — directory move and mode detection

- [x] 5.1 Move `src/session-index.ts` → `src/index/session-index.ts`
- [x] 5.2 Move `src/fts-index.ts` → `src/index/fts-index.ts`
- [x] 5.3 Update import paths across the codebase (one atomic refactor; no shim re-exports)
- [x] 5.4 Bump `INDEX_VERSION` from `3` to `4` in `src/index/session-index.ts`; add `vectorDim: number` field at the root of `IndexData`
- [x] 5.5 Replace v2→v3 migration logic with three explicit cases: (a) file absent → silent init `{version: 4, vectorDim: 0, sessions: {}}`, no notify; (b) file present and `parsed.version === 4` → normal load; (c) file present and `parsed.version !== 4` → hard-reset to `{version: 4, vectorDim: 0, sessions: {}}` AND `DROP TABLE IF EXISTS sessions;` on `sessions-fts.db` AND `DROP TABLE IF EXISTS s;` on `hybrid-fts.db` (both FTS DBs are wiped, not just the JSON index, to prevent v3 raw-content rows from coexisting with v4 digest-content rows in `digest-mode`); recreate the FTS5 virtual tables fresh; AND `ctx.ui.notify("session-search: index version <N> is incompatible; reset to v4. Run /digest:backfill to repopulate.", "info")` once
- [x] 5.6 KEEP `populateFtsFromIndex()` for FTS5 recovery path (when `*.db` is missing/corrupt but `session-index.json` and digests survive). Document in design.md.
- [x] 5.7 Add a `Mode` type: `"fts-raw" | "hybrid-raw" | "digest-mode"` and a `detectMode(config, digestModelResolved): Mode` function
- [x] 5.8 In `index.ts` extension entry: call `detectMode()` after config + model registry are available; pass mode into the index constructor
- [x] 5.9 Add `vectorDim` mismatch detection on load: read current effective `dimensions` from embedder config (or learn from first embed if unset). If `data.vectorDim !== effectiveDim && data.vectorDim !== 0`, mark all sessions dirty for re-embed on next sync; `ctx.ui.notify("session-search: embedding dimension changed; re-embedding all sessions.", "info")`
- [x] 5.10 Add equal-length assertion in `cosineSimilarity(a, b)`: throw if `a.length !== b.length`

## 6. Indexing layer — digest-mode content surface

- [x] 6.1 In `src/index/session-index.ts`: add a `mode` constructor param; `buildEmbeddingText(session, mode, digest)` returns `digest.body` in `digest-mode`, falls back to upstream raw-content concat in `hybrid-raw`. **Regression-pin**: `mode === "hybrid-raw"` MUST return byte-identical output to the upstream `buildEmbeddingText(session)` function on a fixture session.
- [x] 6.2 In `src/index/fts-index.ts`: add `mode` param; `buildContent(session, mode, digest)` returns `digest.body` in `digest-mode`, upstream raw-content in `fts-raw`. **Regression-pin**: `mode === "fts-raw"` MUST return byte-identical output to upstream `buildContent(session)`.
- [x] 6.3 In `digest-mode`, `sync()` SHALL include all discovered sessions in the index metadata (so `session_list` works pre-backfill), but SHALL skip embedding/FTS-content population for sessions where `loadDigest(id) === null`. Un-digested sessions are listable but not searchable.
- [x] 6.4 Add a public `addDigested(sessionId, parsedSession, digest, opts?: {batched: boolean}): void` method on `SessionIndex` (digest-mode + hybrid-raw fallback only). With `batched: true`, the index updates in-memory only and defers the disk write; backfill uses this. With `batched: false` (default), the index saves to disk immediately. `addDigested` also re-embeds `digest.body` for that session and updates the FTS5 row. `FtsSessionIndex` does NOT receive these methods (the mode router never reaches it in digest-mode); document this asymmetry.
- [x] 6.5 Add `flush(): void` method on `SessionIndex` that persists in-memory state to disk; called by backfill every 25 digests + on completion.
- [x] 6.6 Add a `backfillInProgress: boolean` mutex on `SessionIndex`; while `true`, the periodic 5-min `sync()` returns immediately without doing work.
- [x] 6.7 Add `getDigest(sessionId): SessionDigest | null` getter on `SessionIndex` only (renderers in `fts-raw` mode never call it; `FtsSessionIndex` does not implement it)
- [x] 6.8 Update `IndexedSession` type: REMOVE `summary: string`; render code derives display string from `digest.headline ?? truncate(firstUserMessage, 80) + " (no digest — run /digest:update)"`. Add `digest: SessionDigest | null`. **Retain `firstUserMessage: string` (≤200 chars) on the type** — it's the un-digested fallback display source and the parser already extracts it; without retention, render-time would need a fresh `parseSession()` per row.
- [x] 6.9 Migrate `buildSummary(session)` callers in `src/utils.ts` to take an optional `digest` arg and prefer `digest.headline`/`digest.body` when present. When `digest === null` in digest-mode, fall back to `truncate(firstUserMessage, 80)` with a `(no digest — run /digest:update)` suffix. **Use this exact suffix string everywhere** — it must match between `session_list` rows, primer lines, and overlay cards.
- [x] 6.10 Add fixture-pinned regression tests for raw modes: `src/__tests__/fixtures/raw-mode-fixtures/<id>.jsonl` + expected outputs in `src/__tests__/raw-mode-regression.test.ts`. Tests verify byte-identical output of `buildContent(session, "fts-raw")` and `buildEmbeddingText(session, "hybrid-raw")` against captured baselines.
- [x] 6.11 In `SessionIndex.search()` digest-mode path: filter out entries with empty/null in-memory `embedding` (the sync invariant guarantees these are exactly the un-digested ones in digest-mode — NO per-query disk reads) BEFORE cosine scoring. The FTS query path filters out rows with empty `content`. The `cosineSimilarity(a, b)` function (already requires equal-length vectors per task 5.10) SHALL also reject empty vectors with a clear error — search() pre-filters, so this assertion only fires on real bugs.
- [x] 6.12 Verify `node --test` passes after these changes (existing tests may need fixture updates)

## 7. Search tools and primer

- [x] 7.1 Update `session_search` tool render in `src/index.ts`: in `digest-mode`, show `digest.headline` as title; show `digest.topics.join(", ")` as a topic line; show `digest.body` as the body excerpt (truncated)
- [x] 7.2 Update `session_search` empty-state message: when index size is 0 in `digest-mode`, message reads "Session index is empty in digest mode. Run /digest:backfill to digest historical sessions, or wait for new sessions to be digested live." In `fts-raw`/`hybrid-raw` modes, keep the existing "may still be building" message.
- [x] 7.3 Update `session_list` tool render in `src/index.ts`: prefer `digest.headline` over `truncate(firstUserMessage, 60)` when a digest exists; show `(no digest — run /digest:update)` tag for un-digested sessions (matches 6.9 canonical string)
- [x] 7.4 `session_list` SHALL list ALL discovered sessions in digest-mode (not only digested ones), so corpus browsing works pre-backfill
- [x] 7.5 Modify the existing `pi.on('before_agent_start', ...)` handler in `src/index.ts` (currently around line 29): per-session line uses `digest.headline` when available, fallback to `truncate(firstUserMessage, 80)` otherwise
- [x] 7.6 Verify the 1500-char primer cap is preserved
- [x] 7.7 `session_read` tool: no behavior change — verify path traversal guard still works against the `extraSessionDirs` from config

## 8. Slash commands

- [x] 8.1 `/digest:settings` — create global `~/.pi/session-search/digest.json` with defaults if absent; notify user with path + `/reload` instruction
- [x] 8.2 `/digest:update` — call `generateDigest()` for the current session, bypassing debounce; notify on success/failure
- [x] 8.3 `/digest:show` — print the current session's digest as a notification (or widget if `showWidget: true`); show `(no digest yet)` if absent
- [x] 8.4 `/digest:rewrite` — force full re-summarize regardless of threshold; replace on-disk digest atomically; re-embed in index
- [x] 8.5 `/digest:backfill` — iterate **discovered session files via `discoverSessionFiles()`** (not index membership); for each file where `loadDigest(parsedSession.id) === null` AND `parsedSession.id !== ctx.sessionManager.getSessionId()` (skip the active session — it's owned by the live lifecycle), parse via `parseSession()`, build a `parsedConversationView`, run `generateDigest()`, **re-check `loadDigest(id) === null` immediately before `saveDigest`** (race guard: live lifecycle may have written one during the LLM call), save digest, call `index.addDigested(..., {batched: true})`. Set `index.backfillInProgress = true` for the duration. Flush index every 25 digests via `index.flush()`. Render progress via `ctx.ui.setStatus("session-search", "Backfilling digests: <done>/<total>")`. **Wrap the whole loop in `try/finally`**: the `finally` block MUST `index.flush()`, set `index.backfillInProgress = false`, clear status (`ctx.ui.setStatus("session-search", undefined)`), and emit a terminal `ctx.ui.notify(...)` on success/failure/abort regardless of how the loop exits. Per-iteration errors (parse failure, LLM failure) are logged and skipped — they don't abort the run.
- [x] 8.6 `/digest:backfill --dry-run` — enumerate un-digested files (no LLM), compute `inputTokenEstimate = Σ(file.sizeBytes / 4)` summed across files. Output formula uses pi-ai's `Model<Api>.cost` shape: `inputCostUsd = inputTokenEstimate × model.cost.input`, `outputCostUsd = sessionCount × 700 × model.cost.output` (700 = typical observed digest output, NOT the `maxTokens: 1500` ceiling — using the ceiling overestimates by ~2x). Embedding cost is excluded by default since `EmbedderConfig` carries no price field; if the user has set optional `embedder.pricePerInputToken: number` in `config.json`, include `embedCostUsd = sessionCount × 700 × embedder.pricePerInputToken`; otherwise print "embedding cost: not estimated (configure embedder.pricePerInputToken to include)." Print input/output/embed (when applicable) plus total with a note about ±30–50% accuracy bound.
- [x] 8.7 `/digest:backfill --regen` — process all sessions including already-digested ones (overwrites existing digests); same batched/flush/mutex behavior.
- [x] 8.8 `/digest:cost` — call `costTracker.format(rollup, resolvedModelName)` and notify
- [x] 8.9 `/session-embeddings-setup` — flatten to required prompts (`baseUrl`, `model`, one of `apiKey`/`apiKeyEnv`) followed by optional prompts (`dimensions`, extra session/archive directories); remove provider-selection menu; remove bedrock/mistral/ollama-specific branches
- [x] 8.10 Update `/session-sync` and `/session-reindex` — no behavior change beyond inheriting the `Mode` detection through the index; verify existing functionality

## 9. Find-session overlay

- [x] 9.1 Create `src/search/overlay.ts` exporting a `registerFindSessionCommand(pi: ExtensionAPI, deps: {index})` function
- [x] 9.2 Build a card-list TUI component using `ctx.ui.custom(...)` (overlay mode) extending `pi-tui`'s `Component` + `Container` + `Focusable` primitives. Renders one card per result with: `digest.headline` (bold), `digest.topics.join(", ")` (dim), date (right-aligned), `digest.body` excerpt (truncated to 3 lines), file path (dim)
- [x] 9.3 Hook keyboard: ↑/↓ navigate, Enter switches session via `ctx.switchSession(sessionPath)` (verified to exist on `ExtensionCommandContext`), Esc dismisses
- [x] 9.4 Empty-state card when 0 results: "No sessions match this query"
- [x] 9.5 Wire query input: command takes the rest-of-line as initial query; user can edit live
- [x] 9.6 Register the command in `src/index.ts` via `pi.registerCommand(...)`. Overlay calls `index.search(query, 25)` directly (not via the LLM-callable `session_search` tool wrapper) for speed and simplicity. **Lifecycle**: the slash-command handler awaits `ctx.ui.custom(factory)`, which resolves with `sessionPath | undefined` from the factory's `done()` callback; the handler then calls `ctx.switchSession(sessionPath)` if defined, else returns. Esc invokes `done(undefined)`. **Note**: src/index.ts not touched; `registerFindSessionCommand` exported for Phase 10 import.
- [x] 9.7 Add `src/__tests__/search/overlay.test.ts` with: (a) renders cards from injected fake `index` results; (b) ↑/↓ moves selection; (c) Enter resolves the overlay's `done(sessionPath)` callback with the selected path AND the outer command handler then calls `ctx.switchSession(sessionPath)`; (d) empty results → empty-state card; (e) Esc dismisses without invoking `switchSession` (`done(undefined)` returned, command handler short-circuits).

## 10. Extension entry wiring

- [x] 10.1 In `src/index.ts`: import `installDigestLifecycle`, `registerFindSessionCommand`, `loadDigestConfig`, the new `createEmbedder`, the indexes
- [x] 10.2 In `session_start` handler: load both configs (`loadConfig` + `loadDigestConfig`); resolve digest model via `model-resolver`; detect mode; create the appropriate index; install digest lifecycle if mode === `"digest-mode"`
- [x] 10.3 In `session_shutdown` handler: dispose the lifecycle, close the index DB
- [x] 10.4 Register all `/digest:*` slash commands
- [x] 10.5 Register `/find-session`
- [x] 10.6 Update the extension's exported helper API at the bottom of `src/index.ts` to reflect the new module layout (re-exports from `digest/`, `search/`, `index/`)

## 11. Documentation

- [x] 11.1 **Hard release gate**: update `README.md` thoroughly — replace upstream's "How It Works" section with the digest-driven model; replace "Setup" section to reflect the flat 4-prompt embedder configuration (no multi-provider menu); update "Features" section to remove the multi-provider claim; update "Environment Variables" (only `OPENAI_API_KEY` if used as `apiKeyEnv` default; remove `MISTRAL_API_KEY`); add the digest config example; document the three modes (`fts-raw`/`hybrid-raw`/`digest-mode`) explicitly with a small matrix; document all `/digest:*` commands; note the bedrock/mistral/ollama removal under "Migration from upstream"; before tagging a release, the README must show no references to the removed providers and accurately describe the mode matrix
- [x] 11.2 Update `skills/session-history/SKILL.md`: mention `digest.headline`/`digest.body` in tool result shape; mention `/find-session`
- [x] 11.3 Create `CHANGELOG.md` (does not currently exist in repo) with an initial entry crediting `pasky/pi-session-summary` as the design inspiration; note re-implementation from spec (upstream has no LICENSE)
- [x] 11.4 In CHANGELOG: note the upstream divergence — this fork is no longer merge-compatible with `samfoy/pi-session-search`. Document rollback steps explicitly: `git revert` the change AND `rm -rf ~/.pi/session-search/index/` to clear the v4 index file (which a reverted v3 codebase cannot read).

## 12. Cleanup and verification

- [x] 12.1 Scaffold `src/__tests__/fixtures/` directory; capture 3–5 small real session JSONL files (anonymized if needed) for fixture tests — Phase 6 had 1; added fixture-session-002.jsonl + fixture-session-003.jsonl (synthetic, anonymized, 5–10 entries each).
- [x] 12.2 Verify `node --test --import tsx src/**/*.test.ts` passes end-to-end — 212 tests pass, 0 fail.
- [x] 12.3 Smoke-test `/digest:backfill --dry-run` against fixtures dir; verify cost output formula — wrote `src/__tests__/digest/backfill.test.ts` (5 tests) exercising the dry-run formula with {sessionCount:5, totalBytes:50000, input:0.000001, output:0.000005}; all pass.
- [x] 12.4 `npm run` (whatever the test/typecheck combo is) clean — only `test` script exists; passes. No eslint configured (skipped).
- [x] 12.5 Add `@mariozechner/pi-ai` to `peerDependencies` in `package.json` with explicit semver range. **Hard gate**: `npm ls @mariozechner/pi-ai` must resolve a single version with no peer warnings before this task is checked. Read the actual version `pi-coding-agent` 0.66.1 ships with from its `package.json` and use a compatible caret range. — Added `"^0.58.4"` (installed version is 0.58.4). `npm ls @mariozechner/pi-ai` resolves cleanly, no warnings.
- [x] 12.6 Add `@mariozechner/pi-tui` to `peerDependencies` (used by `/find-session` overlay) — Added `"^0.58.4"` (installed version is 0.58.4). `npm ls @mariozechner/pi-tui` resolves cleanly.
- [x] 12.7 Confirm no orphaned imports from removed embedder classes; remove unused `optionalDependencies` entries (`@aws-sdk/client-bedrock-runtime`, `@aws-sdk/credential-providers`) — `grep -rnE "BedrockEmbedder|OllamaEmbedder|MistralEmbedder" src/` returns empty. No `optionalDependencies` block in package.json (already removed by Phase 1).
- [x] 12.8 Confirm `package.json` `version` bump strategy (this is a breaking change vs upstream — bump major to `2.0.0` since this fork is post-upstream-v1.0.0) — Bumped from `1.0.0` to `2.0.0`.
- [x] 12.9 Open an upstream issue against pi-mono requesting `registerSessionSource` for `pi -r` (referenced in design.md as bonus territory) — **Deferred** — requires manual GitHub issue creation by maintainer; the design.md decision references this as bonus territory.
- [x] 12.10 Commit prefix: informational only — since this fork explicitly stops being merge-compatible with `samfoy/pi-session-search` (see Non-Goals in design.md), the `custom:` prefix from the forking-for-custom-patches skill carries no merge-time signal. Use it if you want; not enforced.
- [x] 12.11 Final review: `openspec status` reports 4/4 artifacts complete; all 109 tasks marked `[x]` (Phase 11 docs landed in parallel during Wave 7); `openspec validate --strict` passes; `npm test` → 212/212. Ready for archive via opsx-archive flow.
